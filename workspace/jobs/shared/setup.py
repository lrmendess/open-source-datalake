from setuptools import setup, find_packages

setup(
    name='shared',
    version='1.0.0',
    packages=find_packages(),
    description='Library of common and reusable functions between the project\'s spark jobs.'
)
